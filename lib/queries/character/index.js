const characterIdPage = require('./character-id-page.graphql')
const characterId = require('./character-id.graphql')
const characterSearch = require('./character-search.graphql')

module.exports = {
  characterIdPage,
  characterId,
  characterSearch
}
