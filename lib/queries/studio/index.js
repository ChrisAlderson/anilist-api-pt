const studioIdPage = require('./studio-id-page.graphql')
const studioId = require('./studio-id.graphql')
const studioSearch = require('./studio-search.graphql')

module.exports = {
  studioIdPage,
  studioId,
  studioSearch
}
