{
  "name": "anilist-api-pt",
  "version": "1.0.0",
  "description": "An AniList API wrapper for NodeJS.",
  "dependencies": {
    "debug": "^3.1.0",
    "got": "^8.0.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "coveralls": "^3.0.0",
    "eslint": "^4.12.1",
    "eslint-config-vixo": "^2.0.1",
    "mocha": "^4.0.1",
    "nyc": "^11.4.0"
  },
  "license": "MIT",
  "scripts": {
    "coveralls": "cat ./coverage/lcov.info | node ./node_modules/coveralls/bin/coveralls",
    "lint": "./node_modules/.bin/eslint {examples,lib,test}/** *.js",
    "test": "./node_modules/.bin/nyc --reporter=lcov --reporter=text ./node_modules/.bin/mocha --timeout 15000 --reporter spec test/**"
  },
  "engines": {
    "node": ">=6.3.0"
  },
  "keywords": [
    "anime",
    "anilist",
    "api"
  ],
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/ChrisAlderson/anilist-api-pt.git"
  }
}
